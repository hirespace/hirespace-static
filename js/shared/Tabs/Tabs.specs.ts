module hirespace.specs {
    'use strict';

    describe('Tabs module', () => {
        it('should have the listen method', () => {
           expect(hirespace.Tabs.listen).toBeDefined();
        });
    });
}