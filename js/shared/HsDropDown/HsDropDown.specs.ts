module hirespace.specs {
    'use strict';

    describe('DropDown module', () => {
        it('should have the listen method', () => {
            expect(hirespace.DropDown.listen).toBeDefined();
        });
    });
}